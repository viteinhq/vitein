{
  "name": "vitein",
  "version": "0.0.0",
  "private": true,
  "description": "vite.in — global invitation platform (monorepo root)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/viteinhq/vitein.git"
  },
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20.11",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.3.0",
    "typescript": "^6.0.3"
  }
}
